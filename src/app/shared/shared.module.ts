import { NgModule } from '@angular/core';
import { UserState } from './state/user.state';

@NgModule({
  declarations: [
  ],
  imports: [],
  providers: [UserState],
})
export class AppSharedModule{
}
